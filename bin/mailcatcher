#!/bin/sh

docker run \
    --interactive="$([ -t 0 ] && echo "true" || echo "false")" \
    --net host \
    --rm \
    --tty="$([ -t 0 ] && echo "true" || echo "false")" \
    "${IMAGE:-timonier/mailcatcher}:${TAG:-latest}" mailcatcher "$@"
